# 印·Yinor 运行时依赖（最小集）
# Python >= 3.11（3.13 实测通过）；SQLite/FTS5 走标准库，无需额外安装
#
# 安装：uv pip install -r requirements.txt
#   或：python -m pip install -r requirements.txt

fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.6
numpy>=1.26
python-dotenv>=1.0
